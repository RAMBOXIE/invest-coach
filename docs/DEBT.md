# 欠账登记

> 已裁决但没做完的事，以及会挡住发布的那几条。
>
> **为什么单独一份**：欠账散在各文档的正文里就没人会去数。这份是唯一的清单——
> 任何「先记着，以后做」的东西写在这里，不写在别处。

---

## 会挡住 `poc-trial` 定版

| # | 欠账 | 现状 | 谁能解 |
|---|---|---|---|
| D1 | **14 个节点全部未经人工签字** | `x_prov.reviewed_by` 全空，`validate.py --release` 会拒绝 | 人工逐条过 G1/G2 |
| D2 | **三幕从未过任何内容门禁** | `build.py` 只对 `content/ch1/site.json` 跑 validate；`content/stories/*/case.json`（含真人姓名与逐字原档引文）不在校验范围内 | 给 validate 加 `--stories` 模式 |
| D3 | **隐私条款整条未落地** | 已裁决：提问入库前对金额/证券代码/联系方式打码、90 天定时清理、输入框常驻提示。三条都没做 | server 两侧各加打码函数 + 定时清理；`.askin` 加提示 |

## 已裁决但未执行

| # | 欠账 | 说明 |
|---|---|---|
| D6 | **`content/current/` 是没有消费者的半成品** | 时事管线的产物已提交进 git，但 `build.py` 只读 `content/stories/`。「时事 B 轨」需律师意见，暂缓。当前状态：产物不入构建 |

## 设计与实现欠账

见 [../DESIGN.md](../DESIGN.md) §7「现状与欠账」。摘要：

- 内容正文里的 emoji（`site.json` 的 14 个节点 icon 走 `NODEICON` 映射；
  卡片正文里的 👆📈🏠💧 未动——内容文件不为 UI churn，要清就连内容审校一起做）
- 40 / 56 字号未启用（章节封面与终局还没做）
- `src/template.html` 仍是单文件 1300 行（SPEC_DEV §10 的 #4，未还）
