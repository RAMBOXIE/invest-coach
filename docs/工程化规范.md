# 投资教练 POC · 工程化规范 v2

> **性质**：开发前对齐文档之二，取代 v1（v1 留档）。
> **本版变更（2026-08-22 裁决）**：①零后端 → **轻后端**（匿名遥测 + 「问教练」LLM 代理，成本从简、为未来更多用户留扩展路径）；②dist 先不入 git；③撤销禁词扫描（合规检查转人工 QA）；④教练点评的 LLM 生产方式入规范。工程 7 项裁决全部落定：仓库 `C:\invest-coach`、改造模板、Python 3 工具链、发行双轨、300KB + 微信 WebView。
> **日期**：2026-08-22 ｜ 全部裁决落定，开发门已放行（§10）。

---

## 1. 仓库与目录（已裁决）

独立仓库 `C:\invest-coach\`，名 **invest-coach**，与 xtrader 零耦合（v3 §2.1；不同仓库、不同服务，云资源可复用账号但独立部署）。

```
invest-coach\
├── LICENSE                  # MIT（本仓库）
├── NOTICE.md                # 上游致谢：template 改造自 agent-graph-learning（MIT, © Lichangfocus 2026）
├── README.md
├── .gitignore               # 忽略 dist\（裁决：先不入 git，用时本地构建）
├── docs\                    # 对齐文档正式版 + SCHEMA.md（x_ 字段登记簿）+ 内容作者指南（第 2 步产出）
├── content\
│   └── ch1\site.json        # 唯一内容源（research 冻结版移入）
├── src\
│   └── template.html        # 改造自 learn-map template（文件头保留 MIT 声明）
├── server\                  # ★ 本版新增：轻后端（Go 单模块）
│   ├── main.go              # 单二进制：/api/v1/events + /api/v1/ask-coach + /health
│   └── schema.sql           # 事件表（SQLite/MySQL 双兼容写法）
├── tools\
│   ├── build.py             # site.json 注入 /*__DATA__*/ → dist\index.html（内嵌后端地址常量）
│   └── validate.py          # 门禁校验器（§5）
└── dist\index.html          # 构建产物，gitignore
```

## 2. 前端基线（已裁决：改造）

- 改造上游 template.html（~570 行、零依赖、`const SITE = /*__DATA__*/null;` 注入、DOM+SVG 地图、localStorage）。保留：布局/解锁/节点卡/canon 切换/进度骨架。**剥离：Supabase/账户/云同步**（我们的轻后端是自建极简收集器，不走 Supabase——国内可达性优先）。
- 新增模块（单文件内分节注释）：信心三档、三级提示、「无法判断」判分、错题档案、复训队列、校准统计（Brier Index/敢判率/连胜）、决策转化题型、教练系统（人格切换/出处卡/折叠旁听）、**目录视图**（与地图切换的分组清单）、遥测队列、「问教练」面板（可降级隐藏）、成绩单（兜底保留）。
- vanilla ES2020+，零运行时依赖、零 CDN；**离线契约**：file:// 双击可用，后端不可达时除「问教练」按钮隐藏、数据不上报外，一切功能照常。

## 3. 轻后端（本版核心新增）

**职责只有两个，其余一概不做**（无账户、无内容下发、无判分——判分永远在前端规则引擎，v3 §2.6 不动）：

1. **匿名遥测**：`POST /api/v1/events`——前端批量上报事件（首答/复训/点亮/信心/提示/决策转化/会话时长），载荷 = `{device_id(前端生成 UUID), schema_version, events[]}`。append-only 落库。十人试用的验收指标（≥6/10 完成、复训参与 ≥50%）由此自动统计，成绩单降为离线兜底。
2. **「问教练」LLM 代理**：`POST /api/v1/ask-coach`——请求带 `{device_id, node_id, quiz_id, coach_id, question, 本题作答摘要}`，服务端组 system prompt 调 Claude API 后回传。**护栏（不可协商）**：上下文锁定该题材料 + canon + 学派视角；system prompt 明令禁止前瞻信号、个股买卖建议、收益承诺（v3 §2.3 / v2 §5），超范围问题固定话术拒答；对话全量留日志供人工合规复查；限流：每 device 每日 20 问、每问 max_tokens 封顶、10s 超时静默降级。

**技术选型（成本从简 + 未来可扩，对应 owner「初期规划考虑更多用户」）**：
- **Go 单二进制 + SQLite**（纯 Go 驱动免 cgo）。选 Go 的理由：作者主栈（xtrader 后端即 Go），运维=拷一个文件。
- **扩展路径设计在今天、实现在将来**：DSN 环境变量切换 SQLite→MySQL（schema.sql 双兼容）；events 表 append-only 天然可分区归档；`device_id` 旁预留可空 `user_id` 列（将来接账户不迁表）；服务无状态可横向扩（限流表将来外置 Redis）；API 带 `/v1/` 版本号；载荷带 `schema_version`。
- **CORS 与 file:// 兼容**：请求用简单 POST（`text/plain` 避免预检），服务端接受配置内的站点 origin 与 `null` origin（file:// 双击场景）。
- **部署（已裁决 2026-08-22：免费方案）**：后端进程跑在现有常开机器上，**cloudflared named tunnel**（免费）绑定固定子域出 HTTPS；quick tunnel（trycloudflare，无需账号）地址每次变，仅临时联调。前端把后端地址作为构建常量注入，换地址重构建即可（秒级）。单实例。
- **隐私**：不收任何个人身份信息；about 页一句声明「匿名使用数据用于改进产品」；「问教练」对话日志匿名留存。

**教练点评的两级 LLM 用法**（体验 v2 §1「三层声音」的工程侧）：
- **创作期**：人格点评文本用 LLM 按人格批量生成 → 人工逐条审核 → 静态写入 site.json（运行时零延迟零成本）。生成脚本挂在内容管线里（§6）。
- **运行时**：仅 /ask-coach 一个面。进 v0 还是 v0.5 待 §10.③。

## 4. 数据契约（site.json）

基契约与 `x_` 登记制同 v1（SCHEMA.md 为登记簿，校验器白名单强校验，未登记即 ERROR）。已登记汇总：
- 顶层：`x_note` `x_version` `x_coaches`（含 `canon_sources` 学派出处、`style_lines`；人格字段不用真人名，出处字段必须真名真书）`x_review_bank`。
- 节点：`x_qtype`（合法值含 `决策转化`）`x_level` `x_pairs[].id` `x_coach_notes`。
- 屏/题：`x_covers` `hints×3` `opts[].na` `confidence` `x_pair`。
- **负面契约**：第 1–2 章白名单不含仓位/账户/收益率字段；场景内仓位字段随第 3–4 章设计与法律评估登记；账户级字段远期毕业场前不登记（v3 §2.2/§2.3）。

## 5. 校验器门禁（tools/validate.py，ERROR 清零才放行）

| # | 规则 | 级别 |
|---|---|---|
| 1 | DAG 无环；hard 边层深（ageStart）严格单调 | ERROR |
| 2 | hard 闭包无传递冗余边 | ERROR |
| 3 | ≥1 根节点 | ERROR |
| 4 | canon 四字段非空且 source 命中 sources[] | ERROR |
| 5 | evidence 2–3 条；x_covers 并集全覆盖（第 2 步起） | ERROR |
| 6 | quiz 结构：≥2 选项、≥1 正确；红旗层及以上必有 na + confidence + hints×3 | ERROR |
| 7 | 红旗/案例节点 x_pairs 1–3 对且四字段齐（+id） | ERROR |
| 8 | x_review_bank 覆盖全部 pair id 且 a/b 双面齐 | ERROR |
| 9 | `x_` 字段白名单校验（对照 SCHEMA.md） | ERROR |
| 10 | 节点 name ≤7 字；超长句提示 | WARN |

~~禁词扫描~~（裁决 2026-08-22：不做机器扫描，先把体验做好）→ 收益承诺词、人格字段真人名、页脚声明在位——三项转入 §8 人工 QA 清单必查项。

## 6. 内容管线

第 2 步产出顺序：屏与题文案（含混淆对题、na 选项、hints）→ **LLM 批量生成人格点评 → 人工逐条审核**（审核标准：无前瞻信号、无收益词、贴学派视角、≤字数）→ Sunbeam 数字从 EDGAR 原档核定 → 终局对照公司选定并取数 → validate.py 绿灯 → build。

## 7. localStorage 与遥测事件

- localStorage 同 v1（单 key `invest-coach:ch1`、首答不可变、隐私模式静默降级），新增 `device_id` 与 `coach_id`（当前主教练）、`pending_events[]`（离线暂存，联网补发）。
- 遥测事件类型（v0 封顶 10 种）：`session_start/end` `node_open` `first_answer`（含 correct/confidence/hints_used/na/latency_ms/pair_id）`node_lit` `review_answer` `decision_convert`（若 §10.① 通过）`ask_coach` `chapter_complete` `scorecard_copied`。
- 成绩单保留：完成页/档案页可复制（离线兜底 + 用户自留总结）。

## 8. 测试与验收

- validate.py 绿灯（build 内嵌，不可绕过）。
- 自玩验收：从头玩通，「我学到东西了」成立（v3 §6 第 3 步判定）。
- QA 清单新增：后端关停下全流程无损、「问教练」超范围拒答话术、遥测离线暂存补发、**人工合规三查**（收益承诺词 / 人格字段真人名 / 页脚三句声明）。
- 浏览器矩阵：Chrome/Edge 桌面 + iOS Safari + 微信内置 WebView；dist ≤300KB（已裁决）。

## 9. Git 与版本

- `main` 单干道；Conventional Commits；**提交信息无任何 AI 署名尾注**。
- `dist\` gitignore（裁决：先不存，要发文件时本地 `python tools/build.py` 即得）。
- 版本：内容 `x_version` ｜ 前端 template 头注释 ｜ 后端 `server` 二进制版本号 ｜ 遥测/成绩单 `schema_version` 四轨对齐一次发布 tag（`poc-trial`）。

## 10. 裁决记录（2026-08-22，全部落定）

1. ✅ 决策转化题进第一章（2 案例节点各 +1 题，不动图）。
2. ✅ 章节路线表确认为共识备忘，仓位章法律评估前置。
3. ✅ 「问教练」进 v0（可降级）。
4. ✅ 部署 = **cloudflared 免费隧道**（named tunnel 固定域名为主，quick tunnel 仅临时联调）或其他免费方案；不购置云主机。

**开发门放行。开发顺序**：第 2 步填内容（content/ch1 + 内容作者指南）→ 第 3 步组站（template 改造 + server + build）→ QA 清单 → `poc-trial` 定版 → 十人试用。
