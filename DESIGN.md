# 投资教练 · 设计契约

> 视觉层的单一真源。改任何和外观有关的东西之前先读这份，改完回来更新它。
> 数值真源在 `design/tokens.json`，这份文档只承载**决策与理由**——令牌告诉你"是什么"，
> 这份告诉你"为什么，以及不许怎么做"。

版本：v2.2 · 纸面翻正 + 今日页 IA + 双场补齐 + 点击三态 · 2026-08-26

---

## 0. 一句话

这个产品是**一份可以读的审计案卷**，不是一个 dark mode 学习 app。
所以纸是主场，暗色只留给「幕」（§1）；今日页只给一个下一步，编排由产品承担（§2）。

---

## 1. 核心决策：纸面翻正

### 决策

| | v1 | v2（现行） |
|---|---|---|
| 外壳（目录/进度/判卷台/教练/图谱） | 深夜蓝 `night-800` | **纸 `paper-100`** |
| 卡片 | 白色 5% alpha 浮在暗底 | **`paper-50` 抬起** |
| 幕（叙事沉浸段） | `night-900` | `night-900`（**不变**） |
| 明暗切换的含义 | 无——暗是默认，纸只在"读一幕"里闪一下 | **章节节拍器**：离开纸=进入叙事 |

### 为什么

v1 的问题不是配色难看，是**文案质量和视觉质量严重不匹配**。
「冬天的烧烤架」「你是投委会前唯一读完这份年报的人」「它只用自己写过的话回答」——
这些是编辑级的句子，却被渲染成 20px、和 13px 的 meta 共用同一个字体同一个字重。
落差本身就是"设计很烂"的感受来源。

`tokens.json` 的 `epic_allowlist` 其实早就写对了方向（超大字号阶梯 / 大留白 /
内联 SVG 线条与时间轴 / **原档等宽字体+行号+高亮带**）——那是编辑式设计的配方。
代码一条都没执行。翻正就是去执行它。

### 反面（不许回退成什么）

- 不许把外壳改回暗色"因为看起来更高级"。暗色在这个产品里是**叙事信号**，不是皮肤。
  滥用它，进入幕就不再是一件事。
- 不许给纸面加毛玻璃、霓虹渐变、深色 SaaS 那套。案卷该像案卷。

---

## 2. 今日页信息架构：只给一个下一步

### 决策

调度器 `plan()` 一直在算优先级队列，但 v1 的 `viewToday()` 把队列摊平成 **8 块平级卡片**、
顺序还和队列不一样，首屏同时给出 **4 个平级 CTA**。

**产品的核心价值是「编排」——今天该练什么、上次哪里错了、什么时候该复训。
摊平队列等于把这个决定推回给用户，正好放弃了产品唯一该做的那件事。**

v2 的今日页结构：

```
状态条        进度 + 「复训 N 题到期」（体验 §4「地图顶上」）
             没有数据的指标不渲染——首次进入摆三个「—」是纯噪音
唯一的下一步   队首一件事，暗岛，一个 CTA。教练在这里署名说话
接下来        队列其余项，只有标题与分钟数，**不给按钮**
折叠区        雷区 / 读过的幕 / 换教练的侧重——参考，不是今天的任务
```

队列优先级：**复训（有时效）> 幕（v3 故事驱动的主线）> 节点 > 未签字的实验室**。

### 首个动作是选一件事——但选的是**侧重**，不是人物

「一屏一个动作、没选完什么都不显示」这条 IA 保留。**选的内容被裁决改过。**

v2.0 曾把三个学派人格（账房先生 / 风控官 / 行为教授）提成首屏。
用户裁决（2026-08-26，原文）：

> 「这些 LLM 扮演的人显然就不是『帐房先生』这些原来的设定了，**没有这个选项了**。
> 应该是不同的故事里扮演不同的人，然后在用户的阶段性的故事之后有一个用户专属的教练
> 持续更新对用户的画像……用户可以选择自己的**倾向**，但**不刻板地保留某个模拟的身份**。」

于是 v2.1：

- 首屏问的是「**你希望教练先盯你哪一面？**」，三个选项是 `TONES[].k` / `.d`
  （多讲数字与出处 / 多讲求证与流程 / 多讲我自己的偏差），**不出现人物名**。
- 全站教练一律自称「**你的教练**」（`coachName()`）。
- `site.json` 的 `x_coaches` 保留——它是 `style_lines` 的语气数据源。
  **数据留着，名字不上屏**（同 `site.json` 节点 icon 仍是 emoji 的处理：内容文件不为 UI churn）。
- 顶栏那个「教练」按钮（原来是选学派原型的入口）已删除：原型没了，它就只是一个平级入口。
- 扮演真人的地方只有一个：**幕里的当事人**（`cast[0]`，如 Russell A. Kersh），
  他们只说 `case.json` 里逐字的原档。教练不扮演任何人。

**不许再把学派人格提回首屏。** 这不是设计偏好问题，是已裁决项。

### 教练该在哪说话

体验 §4 里最动人的一句是"一个记得他错误的教练"。v1 把它做成落地页最大的一块卡片
（"我还不了解你" + 三个偏好按钮）——**最强的差异化点被做成了设置面板**。
v2 里教练是暗岛底部的一行署名发言（`profileLine()`），出现在他该出现的地方：反馈。

### 反面（不许回退成什么）

- **首屏不许再加平级 CTA。** 要加入口就进队列，或者进折叠区。
- 不许把进度挪回页面底部。它是"我在哪"，属于顶部。
- 不许在首次进入时渲染空值指标。

---

## 3. 双场系统：纸 与 幕

全站只有两个场。**同一个语义在两个场里有两个值**，这是纪律，不是巧合。

| 语义 | 纸面 | 幕（暗） |
|---|---|---|
| 正文 | `--text-body-color` ink-700 | `--text-narrative-color` paper-50 |
| meta | `--text-meta-color` ink-500 | `--text-meta-on-stage` mist-400 |
| 焦点/强调 | `--state-focus` **amber-700** | `--state-focus-on-stage` **gold-400** |
| 红旗 | `--state-flag` red-500 | `--state-flag-on-dark` #FF8FA0 |
| 压在 gold 填充上的字 | `--on-gold` = `--surface-card`（浅字） | `--on-gold` = `--surface-stage`（深字） |
| 按下叠加 | `--press` 墨色 12% | `--press` 纸色 16% |

**gold-400 在纸上只有 1.35:1，永远不许当纸面强调色。** 纸面强调一律 amber-700（5.73:1）。
这是最容易犯的错，因为 gold 是品牌色，手会自己伸过去。

### 两份值的语义，它的前景色也必然是两份值

`--on-gold` 是 v2.1 补的。只切 `--gold` 不切压在它上面的字，就会出现「深字压深琥珀」：
`.ct-btn.evi`（提出证据）实测 **2.47:1**、`.askin button`（问）实测 **2.79:1**——
同一段 CSS 在幕上完全正确（night-900 压 gold-400 = 13.27:1），一进纸面就塌。

**新增任何「用语义色做填充」的控件时，前景色必须走对应的 `--on-*` 别名，不许写死。**

### 暗岛怎么写

纸面上的暗块（`.big` = 幕的入口）**不要逐条改写后代颜色**，
而是在暗岛容器上把整套简写变量切到幕的一份：

```css
.big{
  --ink:var(--text-narrative-color);
  --soft:var(--text-meta-on-stage);
  --gold:var(--state-focus-on-stage);
  --bad:var(--state-flag-on-dark);
  --line:var(--c-rule-on-stage);
  --card:var(--surface-stage-alt);
}
```

后代自动跟着走。漏掉这一步的后果是实测过的：`.tip` 会在暗底上掉到 **2.46:1**。

### 为什么幕的入口是暗的

`.big` 保持暗色不是装饰。**入口长得像它要去的地方**——纸→幕的转换在点进去之前就被预告了，
同时给整页纸面一处深色重音，解决"全纸面太平"。这是翻正里唯一一个纯设计判断，其余都是纪律。

---

## 4. 字体：一个字族，靠字重与字距拉对比

| 角色 | 令牌 | 用在哪 |
|---|---|---|
| 正文 / UI | `--font-ui` | 一切正文与界面 |
| 展示 | `--font-display`（**和 ui 同一套无衬线**）+ `--weight-display` 700 + `--track-display` -0.02em | 幕标题、章节大字、选教练页 |
| 数字 | `--font-mono` Consolas / SF Mono | 原档数字、行号、EDGAR 引用、进度计数 |

**中文没有拉丁那种"衬线=正文、无衬线=界面"的阅读惯例分工。**
展示层的对比靠 **字号 + 字重 + 字距**，不靠换字族。

### v2.0 在这里犯过一个错，记下来别再犯

v2.0 曾把展示体设成系统衬线（`Songti SC` / `SimSun` / `Source Han Serif SC`），理由是"编辑感 / 案卷感"。
两层都错：

1. **理由本身就是被点名的 AI tell。** `design-taste-frontend` 技能的
   `references/directives-foundation.md` §4.1 有一条 SERIF DISCIPLINE：
   > "It feels creative / premium / editorial" is NOT a reason to reach for serif.
   > The agent's default mental model that "creative brief = serif" is
   > **the single most-tested AI tell in production rounds.**

   规则就在技能里，只是在被拆出去的分片里，写的人没读那个分片。

2. **字体栈在目标机器上根本不存在。** 实测本机（Windows）：
   `Songti SC` ✗、`PingFang SC` ✗、`Source Han Serif SC` ✗、`Noto Serif SC` ✗ ——全是 macOS 专有或需下载。
   展示体一路 fallback 到 **SimSun**（为 12px 点阵渲染设计的宋体），拉到 32px 必然难看；
   连正文首选 `PingFang SC` 也是落到 `Microsoft YaHei`。

**选字体之前先探测目标平台装了什么**，别照抄 macOS 优先的字体栈。
`banned` 里禁的是「自定义中文正文字体」（体积理由）；mono 用系统等宽是功能性的
（原档数字对齐 = 出处可信度的视觉凭证），不算装饰。

> v1 全站只有 1 个字体族且**只有一个字重**。层次缺失的第二个根因不是"少一个衬线"，
> 是没用字重和字距。

---

## 5. 字号阶梯

13 / 15 / 17 / 19 / 24 / 32 / 40 / 56，外加 `--size-display: clamp(32px,9vw,56px)`。

**v1 定义了整条阶梯，实际渲染的最大字号是 20px**（跨度 1.5×）。
v2 幕标题已上到 32px；40 / 56 仍未启用，留给章节封面与终局。

一条硬规则：**meta 13px、UI 15px、正文 17–19px，低于 13px 直接 FAIL**（`check_a11y` A1）。

---

## 6. 令牌纪律与四道门禁

### 那一课，在设计层复发了一次

内容层学过：「机器门禁只查形状不查真值」（见 `docs/records/勘误_第一章.md`）。
设计层原样犯了一遍：

- `tokens.json` 记录 `text.meta` 已修复为 `mist-400`（7.80:1），并注明旧值 `#7b749b` 违反 AA
- 页面实际画的是 template 自建的 `--soft: #6f688a` = **3.24:1**（实测渲染 3.67:1）
- `check_a11y.py` 的颜色对**硬编码在 Python 里**，读的是 tokens，所以一路报 `PASS`

**令牌不被强制引用，真源就不是真源。** 四道门禁是为了让这句话不再成立：

| 门禁 | 查什么 | 修好了什么 |
|---|---|---|
| `check_a11y.py` | A1–A6 同前 · **A7 浮层层级**（抽屉必须高于每个不透明全屏层）· **A8 点击三态**（按下/焦点/禁用）· **A9 死样式**（定义了却没元素会用到的类） | 「渲染了但看不见」「点了没反应」「改了没影响」三类 |
| `validate.py` | 内容 R1–R25 · **R26 目录登记**（对差 `git ls-files`）· **R27 文档链接**（相对链接必须解析得到） | 目录纪律与文档断链，两者过去都没有执行者 |
| `check_tokens.py` | 源码字面色值/字号的**棘轮基线**，只减不增 | 历史债不清零，但不许变多 |
| `check_budget.py` | 体积/性能预算 | 不变 |
| `check_js.py` | J1 `node --check` 跑源码分片 + 产物里每一段内联 `<script>`；J2 扫**被调用却从未声明**的标识符 | **产物跑不跑得起来**——见下 |

### 只比 shell 和 card，等于只比了一半

外壳画的是 `radial-gradient(…, paper-200, paper-100)`——**最深的一端是 paper-200**，
而门禁只拿 `shell`（paper-100）和 `card`（paper-50）去比。于是：

| 令牌 | on paper-100 | on paper-200（渐变深端） |
|---|---|---|
| amber-700 `#8A5E00` | 4.77 ✓ | **4.03 ✗**（顶栏 eyebrow 实测 4.12） |
| red-500 `#B03246` | 5.18 ✓ | **4.38 ✗** |
| blue-500 `#1F6BA8` | 4.71 ✓ | **3.99 ✗** |
| violet-500 `#8646B8` | 4.90 ✓ | **4.15 ✗** |

四个状态色全部只是「刚好够 paper-100」。压深一档之后全部达标，
门禁也改成遍历 `PAPER = shell / shell-alt / card / rule / read / read-alt`
（带 `-on-stage` / `-on-dark` 后缀的走 `STAGE`，两个场不混着比）。

**结论：一个面只要会被真的画出来，就必须进门禁的比较集。**

四道都在 `build.py` 的门禁序列里，任一 FAIL 即构建不合格。

### 第三次撞上同一个模式

court.js 里一处非法的嵌套引号让整个 IIFE 抛异常，`profile` / `TONES` 全部没定义，
今日页渲染成空白——而 a11y / budget / tokens **三道全部报 PASS**。
它们检查的是产物的**形状**（颜色、体积、字面值），没有一道检查产物**跑不跑得起来**。

内容层是出处，设计层是颜色，这里是可执行性。`check_js.py` 补上这一层，
并且排在门禁序列**最前面**——跑不起来就没必要看别的。

### 第四次：`node --check` 只查"能不能解析"

emoji→图标 的整批替换把 `return 📖 出处怎么说` 改成了 `returnic('book')+' …'`——少了一个空格。

`returnic(...)` **语法完全合法**，它只是调用了一个不存在的函数。于是 `node --check` 一路
PASS，`canonLabel()` 一被调用就 `ReferenceError`，判卷台整块挂掉。同一次替换还把
`.chipS.lit` 留在了暗底亮字上，纸面实测 **1:1**——"已读"这个芯片在页面上等于不存在。

J2 因此补上：把产物所有内联 script 合起来，扫**被调用（`name(`）却从未声明**的标识符，
减去浏览器全局与 CSS 函数名白名单。变异测试三种真实写法（`returnic` / 拼错 `profilLine` /
调用已删除的 `pickCoach`）**全部命中**。

J1 查「能不能解析」，J2 查「叫的那个名字存不存在」。**再加门禁前先问：它查形状还是查真值。**

### 内联样式是门禁的盲区，别用

`<div style="background:linear-gradient(...)">` 压过任何样式表规则。
翻正时就栽过一次：CSS 改好了，暗岛始终不生效，因为 JS 模板里有个内联渐变。
`check_a11y` 现在会把内联 style 一并扫进来，但**最好的做法是根本不写**。

---

## 7. 现状与欠账

已完成：

- 纸面翻正（外壳/卡片/底栏/按钮/芯片全部翻到纸面，幕保持暗）
- **幕内部的双场补齐（v2.1）**：`story.css` 一直用 `--text-meta-color` / `--state-focus`
  这两个**纸面**令牌画暗底上的字。实测：退出键 2.78:1、画外音署名 2.97:1、主 CTA 2.96:1。
  改法是在 `.sty` 上挂整套幕的别名、在 `.kn`（幕里的纸岛）上翻回纸面那一份
- 幕入口暗岛 + 32px 标题 + 金色 CTA；`--on-gold` 补齐（§3）
- 全站真实渲染对比度审计：**移动端与 1280px 桌面走完三幕 + 判卷台 + 质问台，无一处低于 AA**
- 令牌管线强制化：**四道**门禁绿，`check_a11y` 状态色改为遍历所有会被画出的面，
  `check_js` 增 J2 未声明标识符扫描
- 状态色压深一档：amber-700 / red-500 / blue-500 / violet-500 全部改为在 paper-200 上也达标
- **质问台补出口**：原来只能点上方 20dvh 的遮罩退出，手机上零提示——用户进了质问就回不到决策。
  现在 `.ct-head` 吸顶常驻「回到决策」，且遮罩关闭也会触发 `onClose`
- 内容里作者手写的 `<details>` 补样式：浏览器默认 21px，远低于 44px 触控下限
- **幕的桌面双栏**（叙事 | 原档对照）：`.ev` / `.derv` / `.doc` / `.trio` 推到右栏，
  `grid-auto-flow: row dense` 回填，**不改 DOM**，所以阅读顺序=无障碍顺序不变
- `--font-mono` 落地到原档数字、行号与派生指标（"看起来可信"的那一处）
- 界面 chrome 零 emoji（26 个描线图标 + `ic()` / `nic()`）
- 桌面布局：底部 tab → 左侧竖栏；今日页双栏；标题 clamp 到 56px
- 展示字体从系统衬线改回无衬线 + 字重/字距（见 §4）
- 今日页 IA 重写：首屏 CTA 从 4 个平级收成 1 个；进度提到顶部；
  首次进入是「选侧重」单屏；教练发言移入暗岛底部（人物名已按裁决移除，见 §2）

新增（v2.2）：

- **点击三态**：全站 `:active` / `:focus-visible` / `:disabled`，`--press` 两份值
- **抽屉层级**：`.sheet` 80 / `.scrim` 79，高于判卷台 40 与幕 60（[ADR-0003](docs/adr/0003-浮层层级.md)）
- **字号全部回到令牌阶梯**：14 / 14.5 / 16 / 18 / 20 / 22 / 44 / 46 八个档外值清零
- **字面值棘轮** 41 色 60 字号 → 1 色 0 字号（剩下那 1 条是 `<meta theme-color>`，
  它不能用 `var()`，只能手动跟 tokens 同步）
- 删死代码：`.big`（IA 重写前的旧入口）、`.labcard` / `.labtag` / `.rev-*` / `.turn` /
  `.ask-av`、「问人物」子系统整块

欠账（按优先级）：

1. **内容里的 emoji 还在**：`site.json` 的节点 icon（14 个，渲染层用 `NODEICON` 映射成 SVG）
   与卡片正文里的 👆📈🏠💧。§7 曾写「页面零 emoji」——**那句话只对界面 chrome 成立**。
   内容文件不为 UI churn（出处纪律优先）；要清就连内容审校一起做。
2. **40 / 56 字号未启用**——章节封面与终局还没做。
3. **质问台的失败反馈会重复**：同一张戳不动的证据卡可以无限次打出同一句回应。
   不影响正确性，但少了一点"他在记着你问过什么"的感觉。
4. **`src/template.html` 仍是单文件 1300 行**。下一刀：`<style>` 切成
   `parts/base.css` + `parts/app.css`，JS 按 today / atlas / me / judge / review 切五片。

其余欠账（含会挡住发布的那几条）集中在 [docs/DEBT.md](docs/DEBT.md)。

### 第五次：没人查「它到底盖在谁上面」

`.sheet` 的 z-index 是 50，幕 `.sty` 是 60、底不透明、铺满 `inset:0`。
于是从幕里唤起的每一个抽屉——**质问台**（旗舰功能）、每一处「核这个数字」——
**DOM 正常、样式正常、事件绑定正常、四道门禁全绿**，
而用户点「先质问」看到的是屏幕纹丝不动。

抓到它靠的不是门禁，是拿 `document.elementFromPoint` 在抽屉的每一个点上打一遍：
返回的全是幕内元素。详见 [ADR-0003](docs/adr/0003-浮层层级.md)。

补的 A7 第一版还犯了同一个毛病：它只看选择器的第一条匹配规则，被
`.stage,.sty{left:96px}` 这条桌面规则骗过去，**把 `.sty` 静默跳过了**——
正是它要抓的那一个。现在扫全部规则，并且自查覆盖数：校验到的全屏层少于 2 个即 ERROR。

**每加一道门禁，都要问它自己会不会静默放行。**

### 点击反馈是设计契约的一部分

`*{-webkit-tap-highlight-color:transparent}` 关掉了浏览器默认的点击高亮，
而全站一条 `:active` 都没有——**手机上点任何东西都是零反应**，长达整个 v2。

现在按元素而不是按类兜底（`button,summary,a[href],[role=button]`），
因为这个站的控件几乎全是 `<button>`，按类列清单迟早会漏。
按下态用 `box-shadow:inset 0 0 0 999px var(--press)` 而不是改 `background`：
它盖在任何底上都成立（外壳渐变、金色填充、纸卡、暗岛、原档引文块），且不参与布局。

A8 强校验三态齐全。

## 8. 改动这份文档的时机

- 改了 `tokens.json` 的语义角色（不是加一个色值，是改一个角色的含义）→ 同一次改动里更新本文档
- 新增一个"场"（目前只有纸与幕）→ 必须先在这里写清它的语义色两套值，再动代码
- 门禁规则变化 → 更新 §6 的表
- 往今日页加入口 → 先读 §2 的「反面」，入口进队列或折叠区，**不许加平级 CTA**
